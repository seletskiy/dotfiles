#!/bin/bash

for i in {0..9}; do
	if xset -q > /dev/null 2>&1; then
		exec /usr/bin/redshift -l55.0167:82.9333 -mrandr
	fi

    sleep 1
done

echo "Failed to start redshift: no response from X11 server."
exit 1
