#!/bin/sh

export TMUX=

if tmux has-session -t mutt; then
    command="tmux attach -t mutt"
else
    command="tmux new -s mutt /usr/bin/mutt"
fi

if [ "$TERM" != "dumb" -a "$TERM" != "linux" ]; then
    exec $command
else
    exec urxvt -e $command
fi
