#!/bin/bash

if [[ $# -lt 1 || "$1" == "-h" ]]; then
    echo usage: "$0 <video.avi>"
    exit 1
fi

exec ffcast -s % ffmpeg -f x11grab -show_region 1 -framerate 25 -video_size %s -i %D+%c -codec:v libx264 -b:v 20000k -vf crop="iw-mod(iw\\,2):ih-mod(ih\\,2)" -y "${@}"
